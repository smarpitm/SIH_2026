================================================================
PRAMANAM - PROJECT CONTEXT
Generated during K0 scaffolding. Keep this file current.
================================================================

== 1. PROJECT SNAPSHOT ==
PRAMANAM is an online verification system for weighing & measuring
instruments under the Legal Metrology framework. Traders apply online,
LMO/GATC officers schedule and perform field inspections, certificates
are issued as signed QR payloads (Ed25519 + JWS) with public badge
verification, plus an offline sticker-parse mode. The app is a Next.js
14 (app router) full-stack monolith backed by Postgres/Prisma, Redis/
BullMQ jobs, and MinIO object storage. All API routes and UI pages are
currently K0 scaffolding (turn-key mock stubs against packages/shared).

== 2. TECH STACK ==
- Next.js 14 (app router, TypeScript, Tailwind, ESLint)
- PostgreSQL / Prisma ORM (v6 pinned, prisma-client-js generator)
- Redis / BullMQ (queues) + ioredis
- MinIO (S3-compatible object store; @aws-sdk/client-s3)
- node:crypto Ed25519 for signature / JWS / QR certificate payloads
- jose (JWT), qrcode, pdf-lib, vitest, tsx

== 3. REPO MAP ==
- app/            Next.js app router: UI pages (Kush) + /api/v1 stubs
- components/     Shared React components: Badge, StatusChip, PhotoInput, CountdownRing
- lib/            Core server libs: db.ts, hooks.ts, hash.ts + owner dirs (auth, uploads, notify, crypto, pdf, i18n)
- packages/shared/        Frozen contracts: constants.ts, types.ts, api.ts, mock.ts
- prisma/         schema.prisma (frozen) + seed.ts
- workers/        BullMQ worker registry (index.ts placeholder)
- root files: package.json, tsconfig.json, docker-compose.yml, .env.example, tailwind.config.ts

== 4. FROZEN CONTRACTS ==
Precedence: PRAMANAM_DECISION_DOC.md is the single source of truth for all implementation decisions.
Interfaces here are NEVER redefined once set: no field removed, no
string identifier re-typed, no mock shape changed without a table-top
sign-off. FROZEN paths:
- packages/shared/constants.ts, types.ts, api.ts, mock.ts
- prisma/schema.prisma (FROZEN at K0, exact model set)
- lib/db.ts, lib/hooks.ts, lib/hash.ts
- docker-compose.yml, .env.example / .env

== 5. STATE OF THE WORLD ==
Phase                           Owner     STATUS     Commands
--------------------------------------------------------------
M1 Auth + session + RBAC       Manav     CODED + GREEN — merged into kush working tree; every flow runs on it; e2e login/register green (tests/smoke+negative 2026-09-03)
M2 Instrument/application/upl  Manav     CODED + GREEN — e2e: multipart instrument create with photo proof, apply NEW, pay RCP-xxxx, submit → auto-alloc all green on fresh DB (tests/smoke.spec.ts 2026-09-03)
M3 Officer queue + inspection  Manav     CODED + GREEN — e2e: /schedule/mine, check-in window, multipart PASS → certificate (tests/smoke.spec.ts); PASS-hook wiring bug fixed by K-sweep (see §7)
M4 Scheduling + notifications  Manav     CODED + GREEN — e2e: CERT_ISSUED notification row asserted in smoke; MA5 invites+bulk-seed typecheck+lint+openapi:check green; /schedule/allocate still K0 stub (chat issue, non-blocking)
M5 Crypto keys/JWS/QR          Smarpit   CODED + GREEN — selftest ALL PASS; smoke asserts 3-segment payloadJws + signatureValid:true
M6 Certificate issue + PDF     Smarpit   CODED + GREEN — issuance now live-verified END-TO-END over HTTP (was only selftested in-process; bundle-isolation bug found+fixed by K-sweep, see §7); pdf-selftest 14/14 + manual-check 21/21; print QA item 5 human-only (phone scan >=30cm)
M7 Public verify + search       Smarpit   CODED + GREEN — badge verdict VALID + 5 anchors asserted in smoke; search-selftest 29/29 + public-selftest 31/31 + S5 MANUAL CHECK all green (2026-09-01); /verify page renders live
S6 Expiry ladder + reminders   Smarpit   MERGED to main — CODED + MANUAL CHECK 16/16 GREEN (workers/manual-check.ts ALL PASS, exit 0, live pg 2026-09-03; rerun converges {flipped:0, reminders:0}); BullMQ repeatable 00:30 IST + ADMIN trigger; openapi 36/36
M8 UI polish + i18n            Kush/Nish CODED + GREEN — loading states on every fetch button + empty states swept; admin dashboard loading/error sentence added (K-sweep 2026-09-03); i18n parity green
K12 Real auth UI + guards      Kush      CODED + GREEN (real login/register/refresh in use; e2e green)
K13 Trader real flows          Kush      CODED + GREEN — full trader write chain now covered by tests/smoke.spec.ts (fresh DB, 2026-09-03)
K14 Officer real flows         Kush      CODED + GREEN — officer check-in + multipart PASS submit now live-exercised end-to-end by tests/smoke.spec.ts (the previously untested full multipart submit)
K5  Verify UI real wiring      Kush      CODED + GREEN (vitest 12/12 incl. G6 grammar + i18n fallback; /verify renders live; tampered-paste verdict covered by components/offline-verify.test.ts)
K15 Admin + finishing touches  Kush      CODED + GREEN (vitest 18/18 + e2e 8/8; admin dashboard loading/error sentence added by K-sweep; /admin KPIs nonzero verified on fresh DB)
E2E demo sweep                 Kush      DONE 2026-09-03 — fresh DB (db:push --force-reset + db:seed), PRD §14 path green end-to-end, tests/smoke+negative 26/26 total, see §7
SMV1 CERT_ISSUED app flip      Smarpit   CODED + GREEN — PASS now completes the lifecycle: workers/index.ts flips PASSED→CERT_ISSUED after successful issuance (detached post-hook poller + applyTransition + app.cert_issued audit); smoke asserts CERT_ISSUED; e2e 26/26 x2 (2026-09-04)
Run commands: npm run dev | npm run build | npm run test |
npm run typecheck | npm run lint | npm run db:push |
npm run db:seed (tsx prisma/seed.ts, idempotent).

== 6. OWNERSHIP MATRIX ==
Module                           Owner    Phase
--------------------------------------------------
lib/auth (jwt/session/rbac)      Manav    MA1
lib/uploads (minio/multipart)    Manav    MA2
lib/notify (notifications)       Manav    MA4
route stubs (auth/instruments/applications/allocate/inspections/dashboards/reports, openapi)
                                 Manav    MA-suite
workers index                    Smarpit  S1
lib/crypto (keys/jws/qr)         Smarpit  S1
lib/pdf (certificate/sticker)    Smarpit  S3 DONE
route stubs (certificates/public/search/.well-known)
                                 Smarpit  S1/S3
all UI pages + components        Kush     K11
lib/i18n (t, en, hi)             Nishka   -
Shared contracts + seed + context maintained by the primary author
to keep one source of truth and avoid merge conflicts.

== 7. RECENT CHANGES ==
- DEMO-READY SWEEP DONE (Kush, branch kush, 2026-09-03): fresh-DB walk of the full PRD §14
  demo path — npm run db:push --force-reset && npm run db:seed, then every step over the live
  dev server via plain-fetch vitest suites. Result: GREEN end-to-end.
  1. tests/ (NEW): helpers.ts (envelope fetch, login, bounded `until` polling — no sleeps),
     smoke.spec.ts (5 tests): login seeded trader ravi → multipart instrument create WITH photo
     proof (1px PNG → MinIO) → apply NEW (preferredDate=today so the auto-allocated slot is
     inside the -2h/+8h check-in window) → pay RCP-xxxx → submit → auto-allocated SCHEDULED →
     lmo.guntur /schedule/mine → checkin → multipart inspection PASS → poll DB: Certificate row
     ACTIVE, certId PRM-CERT-2026-00001, payloadJws 3 segments, app PASSED, CERT_ISSUED
     notification contains certId → public badge verdict VALID + signatureValid:true + exactly
     5 anchors. negative.spec.ts (3 tests), exact { ok:false, error:{code,...} } envelopes:
     cross-district fetch (lmo.krishna GET /applications/{gunturId}) → 403 JURISDICTION_FORBIDDEN
     { required: "district:Guntur", have: "district:Krishna" } (NOTE: the /applications LIST is
     district-scoped so it can never 403 — the DETAIL route is the jurisdiction surface);
     forced issue on a not-PASSED app via POST /certificates/issue (the API's only status-forcing
     surface; there is no PATCH that mutates status) → 409 INVALID_STATE_TRANSITION
     { to: "CERT_ISSUED" } (SUBMITTED is transient — submit auto-allocates straight to
     SCHEDULED, so `from` is SCHEDULED); text bytes renamed proof.jpg → 415
     UNSUPPORTED_MEDIA_TYPE (magic-byte sniff). Tests need the dev server running
     (PRAMANAM_TEST_URL overrides localhost:3000) and fail with an actionable message otherwise.
  2. DEMO-BLOCKING BUG FOUND + FIXED (app/api/v1/inspections/route.ts — Manav/Smarpit paths,
     announced): Next.js compiles instrumentation.ts as its OWN server bundle, so the PASS
     handler registered there lived in a DIFFERENT lib/hooks module instance than the one
     bundled with the /inspections route — inspection PASS ran with ZERO handlers and silently
     issued NO certificate (no cert.issued AND no cert.issue_failed audits; the full multipart
     PASS→issue chain had never been live-exercised). Fix: call registerWorkers() at module
     scope of the route (idempotent; startExpiryQueue is globalThis-cached so no double queue).
     DEVIATION from the "routes never import workers directly" note — forced by bundle
     isolation; instrumentation.ts registration kept as harmless second belt.
  3. CHAT ISSUES (others' paths, one-liners):
     - Manav: POST /schedule/allocate is still the K0 stub returning MOCK.schedules[0] — demo
       path uses auto-allocation inside submit, so non-blocking, but it will return mock data
       if anyone calls it.
     - Manav: Application status never flips PASSED→CERT_ISSUED — RESOLVED by
       SMV1 (Smarpit, 2026-09-04, see §7 log): workers/index.ts performs the
       flip after successful issuance (applyTransition + app.cert_issued
       audit). CROSS-PATH EDIT ANNOUNCED: tests/smoke.spec.ts assertion
       changed from "stays PASSED" to CERT_ISSUED (Kush-authored file —
       comment + assertion only, nothing else touched).
     - Smarpit: public/certificates/lookup matches certId/serial EXACTLY (no contains) — fine
       per contract, just don't demo it with a partial serial.
  4. UI polish (K15 gap): app/admin/dashboard/page.tsx — dashboard fetch no longer silently
     swallows errors: amber error sentence ("Couldn't load the dashboard — sign in as an ADMIN
     and reload the page.") + "Loading KPIs…" state. Everything else already had loading +
     empty states (officer queue, trader lists, notification bell, verify pages, export
     buttons, login/register/apply/officer-job submit buttons).
  5. README: added "Architecture in one paragraph" section, force-reset command, and
     "Demo walkthrough (PRD §14) and end-to-end tests" runbook (fresh DB → npm run dev →
     npm test; what each suite asserts).
  6. VALIDATION: npm run typecheck 0 errors repo-wide; eslint clean on touched files; vitest
     26/26 (18 pre-existing + 8 new); /verify/PRM-CERT-2026-00001 renders 200; register +
     admin KPIs nonzero (verifiedThisMonth 1, officerProductivity LMO Guntur 2) verified live
     on the fresh DB. Tampered-paste TAMPERED verdict is client-side offline verify — covered
     by components/offline-verify.test.ts + lib/crypto tamper selftests.
- S6-FIX (Smarpit, branch smarpit): repo-wide typecheck restored to 0 errors (2026-09-03).
  Root cause: corrupted .prisma client in local node_modules — generated types
  collapsed (keyof Prisma.TransactionClient was never; findMany results decayed
  to any), producing 27 PHANTOM errors (TS7006/TS2339/TS2362) across Manav's
  MA-suite files (lib/auth/allocation+transition, dashboards, schedule/mine,
  reports, notifications, applications, instruments, auth/register, seed-10k).
  Fix: targeted reinstall only (removed node_modules/.prisma, @prisma, prisma;
  npm install; npx prisma generate) — zero source changes needed for those 27.
  Real error the decay had been masking, fixed in workers/manual-check.ts:
  User.district is string|null (schema) so createSeedCert's inspector param is
  widened to district: string|null + coalesced (?? "Guntur"); also 6 eslint
  errors cleaned (audit meta as any -> typed casts, unused cert2/cert3/sweep3
  bindings dropped). typescript stays ^5 (5.8.3 pin tried during diagnosis was
  reverted). Verified: npx tsc --noEmit = 0 errors repo-wide; eslint clean on
  workers/*; manual-check.ts 16/16 PASS (exit 0) re-run post-fix;
  workers/expiry-manual-check.ts ALL GATES GREEN re-run post-fix.
- S6 (Smarpit, branch smarpit): PRD #6 validity tracking + PRD #7 automated
  reminders via BullMQ. Commit: feat(workers): expiry ladder reminders bullmq.
  1. workers/expiry-scan.ts (NEW) — runExpirySweep(): sweeps ALL certificates
     with status in (ACTIVE, EXPIRING_SOON); daysTo = validUntil - now;
     ACTIVE→EXPIRING_SOON at daysTo<=30, EXPIRING_SOON→EXPIRED when
     validUntil<now (EXPIRED wins over amber on the same sweep). DEVIATION
     commented in-file: DECISION DOC T-90/T-7 ladder is reminder-FREQUENCY only
     — statuses stay binary-windowed (T-30) for demo clarity. Idempotent BY
     DESIGN: guarded UPDATE ... WHERE status=<expected> (rerun no-ops, no
     duplicate audits/notifications); each flip -> AuditLog actorKind
     "system:bullmq". Notifications exactly-once per crossing: insert guarded
     by existing (kind, userId, title) — title deterministically embeds certId
     (Notification has NO entityId column; schema FROZEN). REMINDER_T30 ->
     instrument owner on T-30 crossing; kind "EXPIRED" (module-owned, kind is a
     free String column) -> owner + EVERY district LMO on expiry crossing.
  2. startExpiryQueue(): Queue "expiry-scan" + Worker; repeatable via BullMQ v6
     upsertJobScheduler (JobsOptions.repeat was REMOVED in v6) pattern
     "30 0 * * *" tz Asia/Kolkata; globalThis-cached (db.ts reflex) so dev hot-
     reload cannot duplicate; skips gracefully when REDIS_URL unset (npm run
     dev without redis never breaks) + error listeners so redis-down only logs.
  3. workers/index.ts — startExpiryQueue() registered inside registerWorkers()
     (try/catch belt; TODO(S6) removed). instrumentation.ts untouched.
  4. POST /api/v1/certificates/expiry/scan (ADMIN, requireRole) — inline
     single sweep, returns { flipped, reminders }; audits
     certificates.expiry_scan; openapi.json path added (drift guard 36/36).
  5. workers/expiry-manual-check.ts (npx tsx) — 10/10 gates GREEN exit 0 on
     live pg (2026-09-03): T-30 flip + owner-only REMINDER_T30; EXPIRED flip +
     owner+LMO fan-out; audit actorKind system:bullmq x2; rerun
     {flipped:0, reminders:0}; no duplicate notes/audits. Synthetic EXPIRYCHK-
     fixture fully deleted incl. LMO-fanout notes (cleanup matches by title —
     fan-out addresses every district LMO, not just fixture users).
  typecheck: 0 errors in touched files (27 PRE-EXISTING errors in Manav's
  unmerged MA-suite files: lib/auth/allocation, lib/auth/transition,
  app/api/v1/{applications,auth/register,dashboards/officer,instruments,
  notifications,reports,schedule}, prisma/seed-10k). eslint green on all 4
  touched files.
- K12 (Kush, branch kush): replaced all mock auth flows with the real MA1 API — UI files,
  lib/store.ts, middleware.ts only (app/api/v1/auth/** and lib/auth/** untouched):
  1. lib/store.ts — accessToken in zustand MEMORY only; user persisted to localStorage
     (key pm_user); setAuth also sets pm_session (presence=1) + pm_role cookies for the
     middleware UX; logout clears both + localStorage; rehydrate() restores user
     post-hydration (SSR-mismatch safe; Header calls it in a useEffect).
  2. components/api-client.ts (NEW) — api<T>(path, init) fetch wrapper: attaches
     Authorization: Bearer <accessToken>; parses ApiResponse envelope; on 401 calls
     POST /api/v1/auth/refresh ONCE (single-flight — parallel 401s share one refresh so
     MA1 reuse-detection never trips) then retries the original request; on second 401
     clears the store + redirects /login; throws ApiError {code,message,status,details}
     on ok:false. /api/v1/auth/* paths are exempt from the refresh path so login/register
     failures surface as form errors, not redirects. POST retry sends same body — 401
     means never executed server-side, safe.
  3. app/login/page.tsx — real POST /api/v1/auth/login {email,password} → setAuth;
     pm_session is no longer set to the raw accessToken (was a leak; now presence-only
     via store). Role redirect + ?from= kept. Green banner text: "✓ Registered — log in".
  4. app/register/page.tsx — real POST /api/v1/auth/register via api(); redirect
     /login?registered=true.
  5. middleware.ts — pm_session presence check (unchanged) + pm_role bounce:
     /admin→ADMIN, /officer→LMO|GATC, /trader→TRADER (no/wrong role → "/"). Comment:
     // server remains authority; middleware is UX.
  6. components/Header.tsx — user name + separate uppercase role chip; Logout calls
     POST /api/v1/auth/logout → store.logout() → /login (local clear happens even if
     the call fails — never trap the user).
  7. /trader, /officer, /admin + subpages (officer/job, trader/apply, trader/instruments/new)
     all fetch through api() now; mock data still rendered where MA2/MA4 not merged.
     trader/apply fixed to read the REAL POST /applications shape (single DTO, was
     reading created.data[0] from the stub). Public /verify pages left on plain fetch
     (no auth needed).
  8. vitest.config.ts (NEW, @ alias) + components/api-client.test.ts — 6 checks:
     Bearer attach + envelope unwrap; 401→refresh once→retry with new token; second
     401→store cleared + /login + ApiError; ok:false→ApiError code/message; concurrent
     401 single-flight refresh (5 fetch calls total); auth-endpoint 401 does NOT
     refresh/redirect. npx vitest run = 6/6 PASS; npm run typecheck green.
  Commit: feat(auth-ui): real login register session guards.
- K12 follow-up (register UX): "Invalid registration payload" on weak passwords was
  correct server behaviour (MA1 route: password min 8 + regex /\d/) but the form never
  said so. app/register/page.tsx now (a) shows the rule under the Password field —
  amber-highlighted while invalid, (b) pre-checks min8+digit before POSTing, and
  (c) renders the server's Zod details (error.details = flatten()) as readable
  per-field bullet lines (e.g. "Password: Password must contain at least one digit")
  instead of the generic envelope message. Route untouched (Manav's).
  Commit: fix(auth-ui): register password rule hint + readable validation errors.
- K12 follow-up 2 (register TRADER orgName): form state always carried orgName:"" but
  the server schema is z.string().min(1).optional() — empty string FAILS min(1), so
  every TRADER signup 400'd with "OrgName: Invalid input". Fix (app/register/page.tsx
  only): payload omits orgName key unless actually filled (UI collects it for LMO/GATC
  only). UNCOMMITTED — Kush verifying.
  (VERIFIED GREEN — committed ff22c85 by Kush.)
- K13 (Kush, branch kush): trader flows swapped from mock to real endpoints via api-client.
  UI paths ONLY — no app/api/v1/**, lib/auth/**, lib/uploads/**, packages/shared/** changes.
  1. app/trader/page.tsx — real instruments table (serial, category, district/address,
     NEW "Registered" date column from createdAt); mock fraction rings DELETED: validity
     ring + status chip render from an OPTIONAL certificate field on the instrument
     payload (ponytail comment in file — no endpoint lists trader certs yet, public
     lookup still a stub, so rings show "NO CERTIFICATE / ring appears after first PASS"
     until an MA payload carries one); empty state for zero instruments; applications
     list rows are Links to the NEW /trader/applications/[id] detail page.
  2. app/trader/instruments/new/page.tsx — real POST /instruments as multipart
     FormData (name attrs added so DOM feeds FormData) + PhotoInput name="purchaseProof"
     (accept jpeg/png/webp/pdf, 10 MB cap server-side); VALIDATION_ERROR details
     (Zod flatten) mapped to INLINE per-field red errors via NEW export
     zodFieldErrors() in components/api-client.ts; CONFLICT (serial dup) and
     UNSUPPORTED_MEDIA_TYPE surface as top banner message.
  3. app/trader/apply/[instrumentId]/page.tsx — wizard calls the real chain:
     POST /applications {instrumentId,type,preferredDate(ISO from new date input,
     optional),declarationAccepted:true} → POST [id]/pay → POST [id]/submit
     {declarationAccepted:true}; api() throws on ok:false so the chain advances only
     on success; final panel shows StatusChip of the REAL returned status (submit
     auto-allocates → SCHEDULED, NOT SUBMITTED — deviation noted vs task text) +
     "View Status Timeline" button into the detail page. reVerificationReason sent
     only for RE_VERIFICATION.
  4. app/trader/applications/[id]/page.tsx (NEW) — GET /applications/[id]; timeline
     chips DRAFT→SUBMITTED→SCHEDULED→CHECKED_IN→PASSED→CERT_ISSUED (frozen transition
     order) with current ring-highlighted + prior green; FAILED/REJECTED render as an
     extra red chip (side states); reschedule panel when status==="SCHEDULED": reason
     textarea (client min-10 pre-check matching server zod), POST [id]/reschedule
     {reason} → success notice w/ rescheduleCount + refetch;
     RESCHEDULE_BUDGET_EXHAUSTED → "Reschedule limit reached" banner. ponytail note:
     ApplicationDTO has no rescheduleCount so button shows for any SCHEDULED app —
     server budget is authority.
  5. components/NotificationBell.tsx (NEW, mounted in Header for logged-in users) —
     GET /notifications on mount + every open (unread badge = readAt===null count);
     opening marks unread read via PATCH /notifications/read {ids[]} and flips
     readAt locally; dropdown lists title/body/createdAt, unread rows tinted;
     failures degrade silently (bell must never break the page). Notification shape
     typed locally ({id,kind,title,body,readAt,createdAt}) — frozen types untouched.
  6. components/PhotoInput.tsx — label/accept props (was hardcoded "Photo proof"/image).
  Verification: typecheck green; scoped eslint green on all 7 touched files; vitest 6/6;
  LIVE smoke vs dev server (ravi@demo.in): login ok; instruments list 3 rows w/ createdAt;
  applications 0 (empty state); notifications 0 (empty state); FULL WRITE CHAIN: create
  DRAFT → pay RCP-xxxx → submit status SCHEDULED (auto-alloc) → detail SCHEDULED →
  reschedule ×2 ok (count 1→2) → 3rd reschedule HTTP 409 RESCHEDULE_BUDGET_EXHAUSTED
  (exact envelope the UI maps to the limit message). Committed by Kush.
- K14 (Kush, branch kush): officer flows swapped from mock to real endpoints via
  api-client. UI paths ONLY — app/api/v1/**, lib/auth/**, lib/uploads/**, packages/shared/**
  untouched. Contracts consumed per scripts/openapi.json + route sources (read-only):
  1. app/officer/page.tsx — REWRITTEN on GET /api/v1/schedule/mine (was GET
     /applications): jobs show traderName (fallback traderOrg), instrumentSerial +
     category, district (schedule/mine carries none — resolved by serialNumber from
     the officer-scoped GET /instruments list), APP-XXXXXX short ref, reschedule
     count badge, scheduledFor datetime; overdue (server-computed flag) pinned first
     under a red "Overdue — act now" header with red-bordered cards; then Today /
     Upcoming sections; DONE schedules excluded from the queue but counted in the
     header ("N completed"); mock serial fallbacks (ins_wb_9021 etc.) DELETED.
     Cards link to /officer/job/[applicationId]?scheduleId=<id>.
  2. app/officer/job/[applicationId]/page.tsx — REWORKED: loads application +
     instruments for a serial·district·APP-ref header; CHECK IN card when status
     SCHEDULED (needs scheduleId from the queue link) → POST /schedule/checkin
     {scheduleId} → SCHEDULED→CHECKED_IN, form unlocks via refetch; server window
     errors surface verbatim (verified LIVE: outside −2h/+8h → 400 "outside
     check-in window"). Form gates on CHECKED_IN (all inputs disabled + status
     notice otherwise).
  3. Inspection submit is now REAL multipart POST /api/v1/inspections: FormData with
     scheduleId, result, observations JSON (keys from OBSERVATION_CONFIG.default,
     boolean checkboxes + text areas, all locked until check-in), photos (native
     <input type=file name=photos multiple accept jpeg/png/webp capture=environment>),
     gpsLat/gpsLng (REAL geolocation only — fake demo fallback coordinates REMOVED;
     auto-attempt on unlock + manual retry button); PASS → green "Certificate will be
     issued automatically" panel; FAIL → "reason recorded" panel echoing failReason.
  4. 390px one-handed: all action buttons min-h-[44px] (check-in, GPS, PASS/FAIL,
     submit, open-job, back), checklist rows min-h-[44px] touch targets, sticky
     PASS/FAIL bar kept, native inputs throughout.
  5. GPS denied → non-blocking amber banner "Location not captured — you can still
     submit the report."; submission allowed (server decides policy).
  useSearchParams (scheduleId) → component wrapped in Suspense (Next 14 requirement).
  Verification: typecheck green; scoped eslint green (both files); vitest 6/6; LIVE
  smoke (lmo.guntur@demo.in): login ok; /schedule/mine ok (CS-4412 · Ravi Kumar ·
  RESCHEDULED · Sept-5 slot returned with trader/serial/overdue fields);
  POST /schedule/checkin outside window → exact 400 VALIDATION_ERROR "outside
  check-in window" (the UI shows this message). Full multipart PASS/FAIL submit
  NOT live-exercised (needs an in-window slot) — payload written field-for-field
  against the route source; manual check item. Commit:
  feat(officer-ui): real queue checkin inspection submit.
- K13 follow-up (readability + past-date guard): applications list showed raw cuids
  ("App #cmteg92w…", "Target Instrument: cmtegg3k…"). Fix: portal list resolves
  instrumentId → serialNumber from the already-fetched instruments list (primary line
  = serial, secondary = "APP-XXXXXX" cuid tail); detail page does the same via one
  extra GET /instruments and headings "CS-4412 · APP-0NCGXFC". Wizard now rejects a
  past preferredDate client-side ("Preferred date cannot be in the past…") — server
  (MA2) accepts any datetime, so the UI is the only guard; date-input min attr only
  constrains the picker, not typed values. Committed by Kush.
- scaffold committed (K0: init + Prisma + shared + stubs + UI)
- seed added: 6 users + 6 instruments, idempotent (db:seed prints "seeded already" on rerun)
- context.txt created and kept live per maintenance rule.
- MA1 (Manav, branch manav): real auth + RBAC replacing 5 stubs — lib/auth/{jwt,session,rbac,
  refresh-store,cookies,dto,audit}.ts; auth routes register/login/refresh/logout/me rewritten
  (Zod -> jsonOk/jsonErr, closed-set ERROR_CODES only); refresh rotation with in-memory family
  revocation (reuse of older refresh => family revoked + AuditLog auth.family_revoked);
  pm_refresh cookie httpOnly SameSite=Lax (Secure added in production — dev runs plain http);
  AuditLog rows for register/login/refresh/logout/family_revoked; dep added: zod (one-line
  announcement per book). packages/shared/** untouched. typecheck + lint green; MA1 manual
  check green incl. NEGATIVE reuse replay + RBAC jurisdiction shape
  { required: "district:<X>", have: "district:<Y>" }.
- MA2 (Manav, branch manav): instruments + applications + uploads replacing mock stubs —
  lib/uploads/{minio,multipart}.ts (S3 client from env, idempotent ensureBucket + versioning,
  magic-byte sniff only JPEG/PNG/WEBP/PDF, 10 MB cap, UNSUPPORTED_MEDIA_TYPE { details: { reason } });
  lib/auth/transition.ts (applyTransition — INVALID_STATE_TRANSITION { details: { from, to } } EXACT
  shape, needed by MA3 too); DTO mappers toInstrumentDTO/toApplicationDTO in lib/auth/dto.ts;
  routes: GET/POST /instruments (multipart proof -> MinIO, serial+district UNIQUE -> CONFLICT),
  GET/PATCH /instruments/[id] (only address+capacity mutable), POST /applications (DRAFT,
  feeAmount=FEE_PAISA), pay (mock: receiptId RCP-xxxxxx), submit (requires feePaidAt +
  declarationAccepted, DRAFT->SUBMITTED via applyTransition, // TODO AUTO-ALLOCATE-MA3),
  POST /applications/[id]/photos (multipart -> MinIO keys; TRADER owner or assigned officer);
  mock GET list stubs replaced with real scoped lists. packages/shared/** untouched.
  Commit: feat(api): instruments applications uploads state machine.
- MA3 (Manav, branch manav): scheduling + allocation + inspection + PASS hook —
  lib/auth/allocation.ts (pickAllocationOfficer: fewest open ASSIGNED schedules, tie earliest
  createdAt; none -> jsonErr INTERNAL "no officer in district"); submit now auto-allocates a
  Schedule (scheduledFor = preferredDate ?? +7d, assigneeKind = role) and SUBMITTED->SCHEDULED
  (response { status: "SCHEDULED" }, removed MA2 // TODO AUTO-ALLOCATE-MA3);
  GET /schedule/mine (LMO/GATC, joined app+instrument+trader, ordered by scheduledFor, overdue
  flag); POST /schedule/checkin { scheduleId } (assigned officer only, window [scheduledFor-2h, +8h]
  else VALIDATION_ERROR "outside check-in window", SCHEDULED->CHECKED_IN);
  POST /applications/[id]/reschedule { reason min 10 } (TRADER owner, while SCHEDULED,
  rescheduleCount < MAX_RESCHEDULES else RESCHEDULE_BUDGET_EXHAUSTED, bumps count + lastReason,
  scheduledFor = preferredDate ?? +7d); POST /inspections multipart { scheduleId, result PASS|FAIL,
  observations (keys validated vs OBSERVATION_CONFIG.default), photos[], gpsLat?, gpsLng?,
  failReason? } (assigned officer, app must be CHECKED_IN; report stored; PASS ->
  emitInspectionPass(frozen hooks) + CHECKED_IN->PASSED; FAIL -> failReason required + FAILED;
  AuditLog both). Routes stay hook-agnostic — no workers/** import (book MA3 item 7).
  Commit: feat(api): scheduling allocation inspection pass-hook.
- N1 lint debt fix (requested by Manav): removed unused `useEffect` import in
  lib/i18n/useTranslation.ts (zero behavior change) — unblocks `npm run lint` repo-wide.
  Inform Nishka (lib/i18n/ is her path).
- MA4 (Manav, branch manav): dashboards + notifications + exports + OpenAPI —
  lib/notify/notifications.ts (notify(userId, kind, title, body); CERT_ISSUED is written
  DIRECTLY by Smarpit's lib/crypto/issue.ts — no double-write); GET /dashboards/trader
  (DashCounts exact: pendingApplications, verifiedThisMonth, expiringIn30d, slaBreaches);
  GET /dashboards/officer (todaySchedule, overdueCount, stats trio); GET /dashboards/admin
  (kpis:DashCounts + pendencyByDistrict via raw SQL group-by + officerProductivity top5);
  GET /notifications + PATCH /notifications/read {ids[]}; GET /reports/export
  (entity=instruments|applications|certificates&format=csv, ADMIN all / LMO district / TRADER
  own, CSV + UTF-8 BOM + EXCEL-SAFE =+-@ prefix, format=xlsx → 400 "csv only");
  scripts/openapi.json (hand-written OpenAPI 3.0, 32 paths, envelope refs) served by
  app/api/v1/openapi.json/route.ts + scripts/check-openapi.mjs drift guard
  (path count == route.ts count under app/api/v1 minus openapi.json) + npm script
  "openapi:check" (32==32 green). Commit: feat(api): dashboards notifications exports openapi.
- FLAG (Smarpit, merged in MG2 3ba2ec1): `npm run lint` fails on lib/crypto/* —
  keys.ts 7/43/55 require("node:crypto") [DELIBERATE browser-bundle design, add
  eslint-disable comments], manual-check.ts 61 unused 'o', selftest.ts 34 unused 'pair' +
  65 explicit any. lib/crypto/** is frozen to Smarpit — do NOT edit from MA-suite.
- MA4 follow-up: PATCH moved from /notifications to /notifications/read (book-exact path);
  openapi.json updated to 33 paths (33 route files) — drift guard green.
- README.md fully rewritten (root doc, not frozen): overview, E2E flow, features, tech stack,
  setup + env + demo users, scripts, state machine, envelope + error-code table, full 33-path
  endpoint reference, repo layout, crypto section, frozen-contract table, sprint status.
- BOOT FIX (instrumentation.ts — root, not frozen): `next dev` crashed after MG2 because
  Next 14 compiles instrumentation for BOTH nodejs+edge runtimes and the edge variant cannot
  handle node: builtins pulled in via workers -> lib/crypto (UnhandledSchemeError), PLUS the
  file lacked Next's named-export `register()` contract. Fix: named `export async function
  register()` gated on `process.env.NEXT_RUNTIME === "nodejs"` (canonical Next pattern) —
  edge bundle tree-shakes workers away, nodejs bundle registers Smarpit's workers unchanged.
  Verified: dev boots, login OK, dashboards/admin + export + notifications all respond.
- S1 (Smarpit, branch smarpit): crypto core built in lib/crypto/** —
  1. keys.ts — KID frozen "pramanam-2026-08-01"; generateKeyPair() (Ed25519 PKCS8/SPKI PEM);
     loadKeysFromEnv() reads ED25519_PRIVATE_KEY/ED25519_PUBLIC_KEY (base64) or generates an
     ephemeral pair + prints paste-into-.env-once instructions (dev only); publicKeyJwk() →
     { kty:"OKP", crv:"Ed25519", x: base64url(raw 32B) } (exact shape S4 serves, Kush consumes);
     keyFingerprint() = "sha256-" + base64(sha256(publicKeyPem)).
  2. jws.ts — compact JWS EdDSA; canonicalPayload() recursively key-sorts (deterministic bytes);
     signCredential() = header {alg:"EdDSA", kid, typ:"JWT"} + payload {iss:"pramanam:doca",...claims}
     signed with node:crypto — node:crypto loaded LAZILY inside signCredential so the module stays
     importable client-side; verifyCredential() ISOMORPHIC via WebCrypto subtle.importKey("raw",...)
     + subtle.verify Ed25519 (Node ≥20 webcrypto + modern Edge/Chrome — K5 offline page); verifies
     the EXACT three segments received (never re-canonicalizes); returns {valid,payload} |
     {valid:false, reason:"BAD_SIGNATURE"|"MALFORMED"}; single flipped char in any segment → false.
  3. qr.ts — PURE string/base64 (no node:crypto; parseQrPayload client-side safe). buildQrPayload
     → https://<NEXT_PUBLIC_APP_URL host>/verify/offline#pmnm.v1=b64url({v,alg,kid,s:jws}) — signed
     JWS travels inside the QR (anti-copy rule, ID-only QR forbidden). parseQrPayload accepts full
     URL form OR bare JWS, tolerates trailing whitespace, junk → null. qrDataUrl(text) → PNG data
     URL at ECC level Q (G.5 dense-payload budget) for S3's PDF/sticker render.
  4. selftest.ts — `npx tsx lib/crypto/selftest.ts`: 8 cases (keys+jwk shape, header/iss shape,
     genuine verify, payload-flip→BAD_SIGNATURE, signature-flip→false, kid-flip→false, QR round-trip,
     bare-JWS+newline+junk→null) — ALL PASS, exit 0; loads .env itself (tsx doesn't auto-load
     like next dev does).
  KEY CEREMONY DONE: real pair generated, .env populated locally from .env.example with both
  base64 values (.env.example untouched/frozen). Pinned fingerprint for S4 well-known + chat:
  sha256-PO9p7ojLA9c7h35lZ8tG9bzxbLe99ScGm4hpSXwJi6g=
  BROWSER-SAFETY: qr.ts + verify half of jws.ts carry no node:crypto even transitively (keys.ts
  loads node:crypto lazily inside functions so importing KID doesn't pull it client-side).
  NOT TOUCHED (ownership): packages/shared/**, lib/auth/**, all route files. NOTE for Manav:
  repo-wide `npm run typecheck` shows pre-existing errors in lib/auth/* + app/api/v1/* (also
  node_modules/@prisma/client was missing its .d.ts stubs — regenerated). lib/crypto is clean.
  Commit: feat(crypto): ed25519 jws pmnm.v1 qr selftest.
  MERGED to main (fast-forward, main was still 0ef0c1e) — crypto core now on main.
- S2 (Smarpit, branch smarpit): certificate issuance replacing stubs —
  1. workers/index.ts — registerWorkers() with module-level idempotency guard;
     registers issueCertificate via frozen registerInspectionPassHandler
     (lib/hooks.ts untouched). Expiry queue stays a TODO(S6) behind REDIS_URL.
     ANNOUNCED shared-file additions: instrumentation.ts created (ONLY the
     registerWorkers() call) + next.config.mjs got experimental.instrumentationHook:
     true (Next 14.2 loads instrumentation.ts only behind that flag; stable in 15).
  2. lib/crypto/issue.ts — issueCertificate(event): loads Application+Instrument+
     inspector; IDEMPOTENT via certificate.findUnique(applicationId) -> returns
     existing row unchanged; certId = "PRM-CERT-2026-" + 5-digit zero-padded
     sequence derived from MAX(certId) (delete-safe); validFrom = now, validUntil =
     +VALIDITY_DAYS[category] (frozen constants); claims = { sub: certId,
     applicationId, type, instrumentSerial, instrumentCategory, ownerName,
     issuedBy (officer name), issuedByKind, district, validFrom, validUntil };
     signCredential -> payloadJws; buildQrPayload -> qrPayload; Certificate row
     ACTIVE; AuditLog actor=inspector action=cert.issued; Notification row
     (kind CERT_ISSUED) written DIRECTLY via Prisma (no lib/notify import —
     decoupling per book). NEVER THROWS: catch -> console.error + AuditLog
     cert.issue_failed; PASS never fails because issuance failed.
  3. POST /api/v1/certificates/issue { applicationId } — ADMIN or assigned
     officer; idempotent (existing cert returned); requires app status PASSED
     (else INVALID_STATE_TRANSITION {from,to}); same pipeline as hook.
  4. GET /api/v1/certificates/[id] — accepts certId or row id; role-scoped
     owner trader / issuing officer / same-district LMO / ADMIN; returns frozen
     CertificateDTO + payloadJws + qrPayload (+ instrumentCategory, ownerName,
     issuedBy, issuedByKind read from the signed claims).
  5. lib/crypto/issue-selftest.ts — `npx tsx lib/crypto/issue-selftest.ts`:
     self-contained fixture (trader/officer/instrument/PASSED app), 9 checks
     (create, idempotency, WebCrypto verify sub=certId, claims content, QR
     round-trip, tamper flip, VALIDITY_DAYS 365 for COUNTER_SCALE, CERT_ISSUED
     notification), cleans up after itself, exit 1 on fail.
     EXECUTED: ALL PASS (9/9). Postgres provisioned as portable local binaries
     at C:\\pgportable (PG16.15, initdb -U pramanam, trust auth, db pramanam,
     started via pg_ctl on port 5432 — matches .env DATABASE_URL); db:push +
     db:seed green. NOTE: server is not a Windows service — after a reboot run
     C:\\pgportable\\pgsql\\bin\\pg_ctl.exe -D C:\\pgportable\\data -l
     C:\\pgportable\\logfile -o "-p 5432" start (add pgsql\\bin to PATH first).
     Crypto layer green (`npx tsx lib/crypto/selftest.ts` ALL PASS). typecheck
     clean for all S2 files (repo-wide still shows pre-existing lib/auth
     transition.ts error).
  BOOK for Manav: issuance intentionally does NOT flip Application status
  (ownership rule) — PASSED -> CERT_ISSUED app transition needs an owner-side
  flip after successful issuance (applyTransition is ready for it).
  Commit: feat(certs): issue service hook consumer idempotent.
  S2 MANUAL CHECK (lib/crypto/manual-check.ts, kept for re-runs): ALL PASS —
  (1) row ACTIVE, certId PRM-CERT-2026-00001 format, validUntil = validFrom +
  730d for WEIGHBRIDGE; (2) verifyCredential true, flipped payloadJws false,
  modified payload re-signed with a DIFFERENT key false + control (our key,
  same modified payload) true; (3) issue twice -> same certId, ONE row;
  (4) full hook path: emitInspectionPass (as lmo.guntur) -> certificate row
  appears + CERT_ISSUED notification to owner (note: standalone scripts must
  call registerWorkers() themselves — instrumentation.ts only runs under Next);
  (5) typecheck clean for S2 files; console output greps ZERO matches for
  PRIVATE/BEGIN/KEY= (keys.ts ephemeral-print only fires when env keys are
  unset — dev convenience, documented in S1 entry).

- S3 (Smarpit, branch smarpit): PDF layer replacing the two 501 stubs —
  1. lib/pdf/certificate.ts — A4 portrait branded sheet: header PRAMANAM +
     "Certificate of Verification · Legal Metrology Act 2009"; body: the 5
     identity anchors (instrument serial, owner, issued by + kind, category,
     valid until) sourced ONLY from the signed JWS claims (verifyCredential gate
     at render — tampered/unverifiable payload is REFUSED); status word
     (VALID / EXPIRING SOON / EXPIRED / REVOKED / SUSPENDED — 30d window)
     stamped at generation; footer certId + kid + "Verify: scan QR or
     <APP_URL>/verify/<certId>"; TWO QRs side by side (§G.5): variant-A =
     the offline pmnm.v1 buildQrPayload(jws) envelope, variant-B =
     <APP_URL>/verify/<certId> online fallback; both ECC Q at 30mm print size
     (>= 28mm rule). Also A6 sticker renderer: big 46mm variant-A QR + serial +
     category + validUntil + certId.
  2. lib/pdf/store.ts — own S3 client (deliberately NOT lib/uploads — Manav
     path); putVersionedPdf stores at certs/<certId>/v<N>.pdf with N = max+1
     per regeneration (never overwrites) and stores the cert-status in object
     metadata so routes detect stale renders; getPresignedGetUrl(key, 300s);
     idempotent ensureBucket.
  3. GET /api/v1/certificates/[id]/pdf — role-scoped exactly like the cert GET
     (owner/issuer/same-district LMO/ADMIN); regenerates when pdfKey missing or
     HeadObject metadata status != current cert status; responds { url }
     presigned; AuditLog cert.pdf_downloaded.
  4. GET /api/v1/instruments/[id]/sticker — A6 sticker for the instrument's
     ACTIVE certificate; role-scoped (owner / same-district LMO-GATC / ADMIN);
     NEGATIVE: no ACTIVE certificate -> NOT_FOUND envelope. AuditLog
     cert.sticker_downloaded.
  5. lib/pdf/pdf-selftest.ts — `npx tsx lib/pdf/pdf-selftest.ts`: status-word
     matrix, A4 render, tampered-JWS refused at render, versioned MinIO upload
     (key2 = key1+1), metadata carries status, presigned url points at stored
     key + actually GETs the PDF back, sticker render + store. EXECUTED:
     ALL PASS (13/13), exit 0. typecheck clean for all S3 files.
  INFRA: portable MinIO server downloaded to C:\pgportable\minio.exe, started
  against C:\pgportable\minio-data on :9000 (console :9001) with the .env
  credentials (pramanam/pramanam123); bucket pramanam-docs auto-created.
  NOTE: like Postgres, MinIO is NOT a service — after reboot restart via
  `minio server C:\pgportable\minio-data --address :9000 --console-address :9001`.
  PRINT QA (item 5) needs a human: variant-B from screen at 100% zoom,
  variant-A from a phone camera at >= 30cm. No route/typecheck debt added.
     Commit: feat(pdf): certificate sheet dual-qr sticker versioned minio.
   MANUAL CHECK (lib/pdf/manual-check.ts, re-runnable via `npx tsx`): 21/21 gates
   ALL PASS — dual-QR anchors vs JWS claims byte-for-byte, QR payloads decode by
   phone, negatives (unknown -> NOT_FOUND / other-trader -> AUTH_FORBIDDEN),
   positive same-district LMO allowed, re-render after revoke = new vN + old key
   kept + REVOKED stamp in bytes + metadata, audit cert.pdf_downloaded = 3. typecheck
   + lint clean for manual-check.ts; files left in C:\pgportable\manual-demo.
 - [2026-08-31 re-verification, branch smarpit]: S3 PDF task re-validated against
   the frozen contract with infra started from C:\pgportable (pg16 portable via
   pg_ctl + minio.exe on :9000). pdf-selftest 14/14 PASS, manual-check 21/21 PASS
   (exit 0 both). All typecheck errors observed are pre-existing in Manav-owned
   files (lib/auth/transition.ts, prisma/seed-10k.ts) and untracked tmp-* scratch
   files — none in lib/pdf/** or the two S3 routes. Commit bdb57e2 already pushed
   to origin/smarpit with equivalent message "S3: certificate PDF + A6 sticker
   dual-QR versioned MinIO + 21-gate manual-check"; NOT renamed to the contract
   string because it is pushed history (no force-push).
 - [M7 2026-08-31, branch smarpit] Public trust surface (PRD #16) — replaced all
   5 stubs, commit feat(public): badge lookup wellknown revoke stats:
   1. GET /.well-known/pramanam-public-key — publicKeyJwk() from env keys +
      { kid, alg:"EdDSA", keyFingerprint }; public, no auth.
   2. GET /api/v1/public/certificates/[certId] — NO auth -> BadgeDTO. Badge
      contract (also written as a comment in the route for Kush's renderer):
      verdict = status/date-based, priority REVOKED > EXPIRED > EXPIRING_SOON >
      VALID (REVOKED/SUSPENDED from status; EXPIRED = claim validUntil < now OR
      status EXPIRED; EXPIRING_SOON = within 30d — date math is LIVE here
      regardless of the S6 scanner column). signatureValid = INDEPENDENT
      verifyCredential(payloadJws) with env pubkey — NEVER fabricated; a
      tampered DB payload surfaces signatureValid:false => Kush renders red
      "CHECK FAILED — POSSIBLE FAKE" regardless of verdict. Anchors = EXACTLY 5
      {label,value} from signed claims (serial/owner/issued by+kind/valid
      until/category, DB fallback when sig bad). history = last <=5 AuditLog
      rows for the cert as {at,what}. lib/public/badge.ts owns buildBadge.
   3. GET /api/v1/public/certificates/lookup?q= — matches certId OR instrument
      serial; found -> same badge shape; NOT found -> 200 { found:false }
      (amber on page, NOT 404). In-memory 30 req/min/IP -> RATE_LIMITED
      (ponytail note: Redis counter if time allows; map cleared above 5k IPs).
   4. POST /api/v1/certificates/[id]/revoke — ADMIN or issuing officer only;
      Zod { reason min 20 }; status -> REVOKED + revokedAt + revokedReason;
      AuditLog cert.revoked + Notification kind REVOKED to owner; already
      revoked -> CONFLICT with currentReason in details.
   5. GET /api/v1/public/stats — { totalInstruments, activeCerts (ACTIVE +
      EXPIRING_SOON; EXPIRED not active), revokedCerts, lastIssuedAt }, 60s
      in-memory cache.
   6. lib/public/public-selftest.ts — `npx tsx lib/public/public-selftest.ts`:
      31 gates over REAL route handlers + minted tokens (wellknown shape;
      badge verdict matrix via re-signed claims; tampered payload -> honest
      signatureValid:false; 5 anchors vs claims; lookup by serial/certId/
      unknown/missing-q; 31st request RATE_LIMITED; revoke RBAC + Zod +
      CONFLICT-currentReason + audit + notification; stats shape + cache).
      EXECUTED: 31/31 ALL PASS against live pg16 (C:\pgportable).
   BUG FIXED from earlier WIP: lookup route select was missing payloadJws ->
   would have falsified signatureValid:false for EVERY lookup (red CHECK
   FAILED on good certs). Now mandatory with a comment explaining why.
   openapi:check green (34=34), vitest 6/6, eslint 0 errors on M7 files.

 - [M7 2026-09-01, branch smarpit] PRD #11 search & retrieval — replaced the
   search stub, commit feat(search): role-scoped lookup credential artifact:
   1. GET /api/v1/search?q=<1..64> — Postgres-only ILIKE contains (frozen
      schema has no trigram index; // ponytail: pg_trgm GIN index needs a
      migration — schema FROZEN, stays a chat note). Matches instrument
      serialNumber/make/model + Certificate.certId. Optional filters
      district/category (enums) + status (CERT_STATUSES; instruments are
      filtered on their DERIVED status). RBAC scope: TRADER -> own
      instruments/certs only; LMO/GATC -> own district; ADMIN -> all (same
      shape as MA2 GET /instruments). Items { kind:"instrument"|
      "certificate", id, title, subtitle, district, status, url } with url ->
      /trader/instruments/[id] (instrument status derived from latest cert,
      "UNCERTIFIED" when none — no status column in frozen schema) or
      /verify/[certId]. take 25/kind. NEGATIVES: missing/empty/whitespace q
      and >64 chars -> VALIDATION_ERROR; bad enum -> VALIDATION_ERROR.
   2. GET /api/v1/public/certificates/[certId]/credential.json — OpenCerts-
      style machine artifact { jws, jwk, kid, fetchedAt }: jws = RAW compact
      JWS byte-for-byte as issued (consumer verifies — same honest trust
      model as badge), jwk = current Ed25519 OKP JWK, kid = pramanam-2026-
      08-01, fetchedAt = ISO. NO auth; rate-limited like lookup (shares the
      consumeLookup bucket, 30 req/min/IP -> RATE_LIMITED). Unknown -> 404.
   3. lib/search/search-selftest.ts (`npx tsx lib/search/search-selftest.ts`)
      — 29 gates over REAL route handlers + minted tokens against live pg16:
      q validation negatives, AUTH_REQUIRED, RBAC scoping matrix (trader A/B,
      LMO Guntur/Krishna, ADMIN), make/model/certId matching, item contract
      urls/statuses, district/category/status filters, credential shape +
      byte-identical jws + 404 + 31st-request RATE_LIMITED, and the p95
      sanity (50 queries, console.time). Idempotent: purges prior srchst
      fixtures first. EXECUTED: 29/29 ALL PASS (2026-09-01, live pg).
   4. p95 sanity printed for PR notes: 50 queries, p95-loop-50 ~95 ms total,
      avg 1.9-2.2 ms/query (target < 500 ms) against the seeded dataset.
   5. openapi.json: /search summary/params refreshed (auth + q 1..64 +
      filters) + new /public/certificates/{certId}/credential.json path —
      drift guard green (35=35). vitest 6/6, eslint exit 0 on all three
      files; typecheck clean for M7 files (global still fails only on
      pre-existing Manav files).

- K0 verified (Step 14): docker infra up (pg/redis/minio healthy), db:push in
  sync, db:seed 6 users, typecheck 0 errors; dev server boots; landing renders;
  /api/v1/public/certificates/PRM-CERT-2026-00001 -> ok:true VALID 5 anchors;
  /api/v1/dashboards/admin -> pendingApplications 4.
- K11 (Kush, branch kush): upgraded UI pages from skeletons into a polished, responsive UI:
  1. Global shell in app/layout.tsx with responsive Header (wordmark PRAMANAM, nav links,
     language toggle calling setLang from lib/i18n), and statutory demo build footer.
  2. Role-aware route stub in middleware.ts guarding /trader, /officer, /admin on pm_session cookie.
  3. Login page with working form, POST /api/v1/auth/login, error alerts, pm_session cookie,
     Zustand store (added zustand — announced), and role redirection.
  4. Register page with dynamic role select, conditional orgName for non-traders, district select,
     and success redirect with green banner.
  5. Trader portal with instrument table, StatusChip, CountdownRing validity cards (0.9/0.5/0.1 mapping),
     and applications list.
  6. Application wizard /trader/apply/[instrumentId] with 3-card steps, step indicator, declaration checkbox gating,
     and pretty-printed returned JSON success panel.
  7. Officer queue (/officer) with mobile-first 390px-compatible application cards and Open job links.
  8. Officer job page with inspection form, sticky PASS/FAIL action bar, and mandatory failure reason requirement.
  9. Public verify page /verify/[certId] with hero Badge verdict (15vh min mobile height), definition list anchors,
     history in details, and typed-ID fallback lookup input.
  typecheck + lint green.
-[N1 Completed] - i18n module implemented and verified.
- Owned paths updated: lib/i18n/** (en.ts, hi.json, useTranslation.ts, index.ts, check-parity.ts).
- Translation keys frozen (~40 keys). Parity check script passed.
- Hook contract re-exported from lib/i18n/index.ts: { useTranslation, en }.
- Ready for Kush (K5) import.
- K5 (Kush, branch kush): verify UI wired to real M7 endpoints + offline validator + i18n.
  OWNERSHIP NOTE: lib/i18n/useTranslation.ts extended in place (Nishka's path) because the
  N1 hook on main was still a pass-through stub — export contract { t, lang, setLang }
  unchanged, key set untouched (parity check green), English fallback per N1. en.ts value
  verdict.tampered aligned to the frozen G6 string "CHECK FAILED — POSSIBLE FAKE" (key set
  unchanged). Inform Nishka.
  1. app/verify/[certId]/page.tsx — real GET /api/v1/public/certificates/[certId] BadgeDTO.
     G6 grammar hero: green VALID + "SIGNATURE VERIFIED" sub-line, amber EXPIRING SOON,
     red EXPIRED/REVOKED, red "CHECK FAILED — POSSIBLE FAKE" whenever signatureValid=false
     (verdict-independent) + next-action line "Report to your Local Legal Metrology office".
     Icons accompany every colour; aria-live="polite" hero; ≤5 anchors (server sends exactly
     5; slice(0,5) defensive) with labels translated via verify.anchors.* keys; history in
     <details>. NOT_FOUND renders AMBER "No record found — check the ID printed on the
     certificate" (never a raw 404). Typed-ID input navigates /verify/[id].
  2. components/verdict.ts (NEW, pure .ts) — verdictView(verdict, signatureValid) G6 mapping
     (tone/icon/wordKey/subKey), unit-tested in components/verify-ui.test.ts. Badge.tsx gained
     a display-only CHECK_FAILED config + word/subtitle overrides.
  3. app/verify/offline/page.tsx — REAL client-side verification: textarea accepts pmnm.v1
     envelope or bare compact JWS via Smarpit's parseQrPayload (lib/crypto/qr), verified with
     verifyCredential (lib/crypto/jws, WebCrypto Ed25519). Pubkey fetched ONCE from
     /api/v1/.well-known/pramanam-public-key, cached in localStorage (pm_pubkey_jwk; ponytail:
     cache never expires, key rotation bumps the key per G.4). After the first key fetch the
     verify path makes ZERO network calls. Verdicts: green = signature ok + signed-claim
     validUntil in future; red = BAD_SIGNATURE/MALFORMED or expired-per-claims; amber =
     signature ok but validity unknown offline (no readable validUntil) or key not yet cached.
     Decoded signed claims shown on green/amber/red-with-claims.
  4. lib/i18n — t() now resolves en.ts/hi.json (resolveJsonModule), missing hi keys fall back
     to English then caller fallback; ?lang=hi URL param wins on load, toggle choice persists
     in localStorage (pm_lang); module-level subscribers so the Header toggle flips mounted
     pages live.
  VALIDATION: npm run typecheck 0 errors; eslint 0 errors on touched files; vitest 12/12
  (6 new: G6 verdictView matrix + translate fallback matrix); i18n parity check green.
  Commit: feat(verify-ui): real badge offline validator i18n.
- MA5 (Manav, degradable): invites + bulk seed — cut-first per book:
  1. POST /api/v1/auth/invite (ADMIN only) — creates LMO/GATC user with fixed
     temp password Invite@123 + "share credentials offline" banner; audits
     auth.invite; 409 on existing email. openapi.json path added (drift guard green).
  2. prisma/seed-10k.ts (npm run seed:10k) — 10k instruments + ~1.2k applications
     via createMany batches; status split 60% CERT_ISSUED / 20% SCHEDULED /
     10% SUBMITTED / 10% FAILED; 30 bulk traders (10/district) never log in
     (disabled argon2 hash as passwordHash); idempotent via BULK- serial prefix.
  3. DEFERRED (per book degradability): xlsx export — no 1-dep SheetML writer
     installed, stays csv-only with ponytail note; PATCH /auth/me prefs — User
     has no meta column + schema frozen, ponytail note left in place.
  typecheck + lint + openapi:check green.
- S5 MANUAL CHECK (lib/search/manual-check.ts, runnable via `npx tsx`):
  All 4 verification gates ALL PASS (exit 0) on live PostgreSQL:
  1. Admin vs LMO Scoped Search (q="Essae"): Admin finds all Essae instruments across
     districts (Guntur & Krishna); LMO Krishna finds ONLY Krishna district rows (count logged).
  2. NEGATIVE Checks: Cross-district serial probe by LMO Guntur for Krishna serial returns
     0 rows (NOT a leak); query with 100 chars (>64 limit) returns VALIDATION_ERROR.
  3. Stats Integrity: /public/stats matches direct SQL / Prisma counts exactly (totalInstruments,
     activeCerts, revokedCerts, lastIssuedAt).
  4. Credential & JWK: /public/certificates/[certId]/credential.json parses valid JWS payload,
     WebCrypto signature verifies, kid matches "pramanam-2026-08-01", and jwk/kid matches
     /.well-known/pramanam-public-key exactly.
  All files lint clean (0 errors) and openapi:check 35/35 in sync.
- S6 MANUAL CHECK (workers/manual-check.ts, runnable via `npx tsx`):
  All 4 verification gates (16 sub-gates) ALL PASS (exit 0) on live PostgreSQL (2026-09-03):
  1. Past-Due Expiry & Automatic Red Badge: Seeded cert with validUntil = now - 1d; manual scan
     via POST /api/v1/certificates/expiry/scan flips status to EXPIRED; AuditLog recorded
     with actorKind system:bullmq; public verify route responds verdict: EXPIRED; live browser
     renders red Hero Badge ("Expired Certificate", ✕ icon, red rose-600) WITHOUT redeployment
     (verified via browser screenshot).
  2. Expiring-Soon Crossing & Notification: Seeded cert with validUntil = now + 20d; scan flips
     status to EXPIRING_SOON; exactly ONE REMINDER_T30 notification row inserted for owner.
  3. Back-to-Back Idempotency: Running scan twice back-to-back produces { flipped: 0, reminders: 0 }
     both sweeps; row diff shows exactly 0 duplicate audit rows and 0 duplicate reminder rows.
  4. NEGATIVE Boundaries & Correction: Boundary cert (validUntil = now + 30d + 2h, daysTo = 31)
     unaffected (status ACTIVE, zero flips); simulated forced SQL flip (ACTIVE -> EXPIRED) on a
     T-20 cert is NOT healed by public read queries (remains EXPIRED in DB), and is corrected
     to EXPIRING_SOON ONLY by the scanner's own logic with full audit trail.
  All files lint clean (0 errors) and openapi:check 36/36 in sync.
- K15 (Kush, branch kush): admin + finishing touches — UI paths only (no API/route files touched):
  1. app/admin/dashboard/page.tsx (NEW) — real GET /dashboards/admin payload
     { kpis: DashCounts, pendencyByDistrict, officerProductivity } (the old /admin page
     had wrongly typed the whole payload as DashCounts): KPI grid reads dash.kpis;
     district pendency table rendered only when pendencyByDistrict present (task's
     "if payload includes it"); invite-user miniform → POST /api/v1/auth/invite
     (MA5 SHIPPED the endpoint so NO ponytail hiding): name/email/role(LMO|GATC)/
     district(DISTRICTS)/orgName, success banner shows returned tempPassword +
     banner, Zod fieldErrors surfaced via zodFieldErrors. app/admin/page.tsx is now
     a server redirect("/admin/dashboard") — single implementation, no dup KPI grid.
     officerProductivity left unrendered (not in the task list — data still in payload).
  2. components/export-url.ts (NEW, pure .ts) + components/export-buttons.tsx (NEW) —
     ExportButtons used on /admin/dashboard + /trader: authenticated fetch with Bearer token
     and automatic 401 refresh, applies Content-Disposition filename from header, triggers
     clean blob download with instant completion feedback. buildExportUrl unit-tested in
     components/export-buttons.test.ts (2 tests).
  3. Print CSS: app/globals.css @media print — @page A4 margin 14mm; header, footer,
     .no-print hidden; body and container backgrounds forced clean white/black; main padding/max-width
     reset; shadows removed. app/verify/[certId] gains a no-print "Print certificate handout" button
     and no-print on the typed-lookup card → printed page doubles as the certificate
     handout. app/trader/instruments/[id]/page.tsx (NEW) — minimal instrument record card
     (GET /instruments/[id]) with Print button + Apply link, prints clean A4.
  4. app/officer/page.tsx — now also fetches GET /dashboards/officer; consumes
     todaySchedule.length ("Today's schedule: N" chip) + overdueCount (red/green
     "Overdue: N" chip) ONLY when the payload includes them (optional chaining,
     endpoint failure silently skipped).
  5. Trader page: serial numbers now Link to /trader/instruments/[id] (matches the
     M7 search item contract url).
  VALIDATION: npm run typecheck 0 errors; vitest 18/18; next lint 0 errors/warnings;
  all manual checks verified. NO route/API/frozen files touched.
  Commit: feat(admin-ui): kpi exports print css.
- S6 merged to main (2026-09-03) — MG3 gate COMPLETE (MA4 + S5 + S6).
- SMV1 (Smarpit, branch smarpit): flip Application PASSED -> CERT_ISSUED after
  successful issuance — closes the LAST K7 chat issue (§7 item 3, above).
  Commit: fix(workers): flip application PASSED to CERT_ISSUED after successful issuance.
  1. workers/index.ts — after issueCertificate(event) resolves to a non-null
     certificate, the lifecycle is completed via applyTransition (lib/auth/
     transition consumed, not redefined) + one AuditLog row: action
     "app.cert_issued", entity "application", entityId = applicationId,
     meta { certId }. CRITICAL ORDERING FINDING: the /inspections route
     commits CHECKED_IN -> PASSED only AFTER emitInspectionPass resolves, and
     its PASSED update is unconditional (built from a stale CHECKED_IN object)
     — so an INLINE flip inside the handler would be overwritten back to
     PASSED (and while the handler runs the app is still CHECKED_IN, so
     PASSED->CERT_ISSUED is refused). The flip therefore runs DETACHED
     (void + .catch — the "never throws upward" hook contract is preserved):
     it polls every 5ms (5s cap) until the route's PASSED commit lands, then
     does PASSED -> CERT_ISSUED + the audit row. Idempotency: applyTransition
     only allows the move from exactly PASSED, and an already-CERT_ISSUED app
     exits early — a duplicated PASS event is a harmless no-op (never a second
     audit row). issueCertificate null (issuance failed) -> no flip; the app
     ends at PASSED and the cert.issue_failed audit row is the failure signal.
  2. lib/crypto/issue.ts — stale header NOTE updated: the flip now happens in
     workers/index.ts after successful issuance (was "stays with MA-suite").
  3. CROSS-PATH EDIT ANNOUNCED (Kush-authored tests/smoke.spec.ts — announced
     here per the ownership rules): the assertion at ~142-145 changed from
     "app stays PASSED" to polling for CERT_ISSUED (until() + status-filtered
     findFirst; comment + assertion only — NOTHING else in the tests touched).
     The poll is needed because the flip lands moments AFTER the route's
     PASSED commit, so a single read races the detached poller.
  4. MANUAL CHECK (live dev :3000, fresh DB via prisma db push --force-reset
     --skip-generate + db:seed; MinIO had to be restarted with the .env root
     creds pramanam/pramanam123 — a start without them broke instrument
     uploads with InvalidAccessKeyId -> 500, unrelated to this fix):
     npm run typecheck 0 errors repo-wide; eslint clean on touched files;
     npm test 26/26 TWICE — the second run on the shared DB doubles as the
     double-event/rerun convergence check (new PRM-CERT-2026-00002 created,
     exactly one app.cert_issued audit per application, no duplicate
     schedules/certs/audit errors); DB spot-check: PRM-CERT-2026-00001 cert
     ACTIVE + app CERT_ISSUED + app.cert_issued {certId} audit; negative
     battery intact (forced issue on a SCHEDULED app still 409
     INVALID_STATE_TRANSITION); public badge verdict VALID; trader UI detail
     page as ravi@demo.in shows the CERT_ISSUED chip highlighted in the
     STATUS TIMELINE (Playwright screenshot check).
--------------------------------------------------------
MAINTENANCE RULE
Every task STARTS by reading context.txt and ENDS by updating
section 5 (STATE OF THE WORLD) and section 7 (RECENT CHANGES).

