# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/.next-build/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# local env files
.env
.env*.local
env.vercel.*

# local infra data
/pgdata
/miniodata
/.minio-data/
/.tools/

# verification screenshots are tracked; keep the archive out
/screenshots.zip

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local working docs / artifacts kept out of the repo
/AGENTS.md
/BOOK_MANAV_API_CORE.pdf
/PRD.pdf
/DEPLOYMENT.md
/PROMPTBOOK_PHONE.md

# local helper scripts that embed real credentials — never commit
/scripts/check-b2.ts
/scripts/smoke-s3.ts
/scripts/check-env.ts

.render-secrets.local.txt

# local runtime data (postgres/minio data dirs, scratch)
.local-postgres/
.local-services/
tmp-*.ts
tmp-*.txt
test-output.txt
fulltest*.txt
lint*.txt
tc*.txt
pa-remote.md

# mobile (Expo/React Native) - deps, caches and native build outputs only;
# mobile/src + config stay trackable
mobile/node_modules/
mobile/.expo/
mobile/.claude/
mobile/android/.gradle/
mobile/android/.kotlin/
mobile/android/build/
mobile/android/app/build/
mobile/android/local.properties
.neon
/NSUT-SIH-DEMO/

# dev/agent tooling — never part of the submitted repo
/.agents/
/neon.ts
/skills-lock.json
/MINIO_PUBLIC_ACCESS.md
/scripts/check-db.ts
/scripts/minio-public-tunnel.cmd

# internal working docs — kept local, never part of the submitted repo
/context.txt
/PRAMANAM_DECISION_DOC.md
/PROJECT_AUDIT.md
